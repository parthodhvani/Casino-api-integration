{
  "name": "jackpot-mqtt-listener",
  "version": "3.2.1",
  "private": true,
  "description": "Controllable MQTT listener (Node 14+ / cPanel + LiteSpeed) that forwards DRGT jackpot messages to WordPress sites.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node test/run-tests.js"
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "abort-controller": "^3.0.0",
    "dotenv": "^16.4.7",
    "mqtt": "^4.3.7",
    "node-fetch": "^2.7.0"
  }
}
