{
  "name": "jackpot-worker",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that authenticates, validates, signs and fans out jackpot updates to WordPress sites, and proxies MQTT start/stop/status control.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
