{
  "name": "jackpot-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that signs and fans out jackpot updates to WordPress sites.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
